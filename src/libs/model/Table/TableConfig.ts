import { CustomLabelBoolean } from "../HbsMarkdownHelper/HbsMarkdownHelper";

export interface TableConfig {
  customLabelBoolean: CustomLabelBoolean;
  customFormatDate: string;
  customTextLink: string;
}
